[all]
[all:vars]
#primary_host     = ""        # Require.
#primary_port     = 3306      # If no set, default primary_port is 3306.
#primary_user     = ""        # if no set, default primary_user is replica.
#primary_password = ""        # if no set, default primary_password is randomly generated by ansible

