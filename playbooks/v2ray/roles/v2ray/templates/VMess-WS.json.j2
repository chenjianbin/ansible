{
  "inbounds": [
    {
      "tag": "VMess-WS-{{subdomain}}.{{ zone }}.json",
      "port": {{ port }},
      "listen": "0.0.0.0",
      "protocol": "vmess",
      "settings": {
        "clients": [
          {
            "id": "{{ uuid }}"
          }
        ]
      },
      "streamSettings": {
        "network": "ws",
        "security": "none",
        "wsSettings": {
          "path": "/wsv3",
          "headers": {
            "Host": "{{ subdomain }}.{{ zone }}"
          }
        }
      },
      "sniffing": {
        "enabled": true,
        "destOverride": [
          "http",
          "tls"
        ]
      }
    }
  ]
}
